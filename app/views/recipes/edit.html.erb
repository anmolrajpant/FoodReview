<%= form_for(@recipe) do |f| %>
	<%= f.label ("Recipe") %>
	<%= f.text_field :title %><br>
	<%= f.label ("Ingredients") %>
	<%= f.text_area :items %><br>
	<%= f.label ("Procedure") %>
	<%= f.text_area :process %><br>
	<%= submit_tag ("Save") %>
<% end %>

